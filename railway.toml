[build]
builder = "nixpacks"

[deploy]
startCommand = "FLASK_APP=wsgi.py python startup.py && flask db upgrade && gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/health"
healthcheckTimeout = 100
